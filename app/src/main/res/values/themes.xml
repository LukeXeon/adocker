<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Andock" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color - Docker blue -->
        <item name="colorPrimary">#0DB7ED</item>
        <item name="colorPrimaryContainer">#0DB7ED</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#066DA5</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Andock.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#DCEDC8</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_animated</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.Andock</item>
    </style>
</resources>
